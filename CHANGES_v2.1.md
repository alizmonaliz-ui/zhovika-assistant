# ZHOVIKA — تغییرات این نسخه

## چی واقعاً خراب بود (و چرا دستیارت ضعیف به نظر می‌رسید)

1. **دستیار اصلاً از AI واقعی استفاده نمی‌کرد.** در `index.html`، تابع `processAIChat`
   قبل از هر چیز پیام کاربر را با چند کلمه‌ی کلیدی («ورزش»، «درس»، «زبان»، «کار») چک
   می‌کرد و اگر تطبیق پیدا می‌شد، مستقیم می‌رفت سراغ یک تابع با متن و اعداد از پیش
   نوشته‌شده (`planWorkoutChat`, `planStudyChat`, ...) — بدون اینکه اصلاً به سرور یا
   Claude وصل بشه. فقط وقتی هیچ‌کدوم از این کلمه‌ها در پیام نبود، سراغ `/api/chat`
   (که واقعاً AI بود) می‌رفت. یعنی دقیقاً همون بخش‌هایی که مهم‌ترن (ورزش، درس، زبان،
   وظیفه) هیچ‌وقت واقعاً به Claude نمی‌رسیدن.

2. **حتی وقتی به بک‌اند می‌رسید، Claude درست صدا زده نمی‌شد.** در `main.py`،
   `tools` به Claude پاس داده می‌شد ولی پاسخ آن با جستجوی کلمه در متن (`"برنامه ورزشی" in content`)
   بررسی می‌شد، نه با خواندن واقعی بلوک‌های `tool_use`. یعنی حتی اگر Claude درست کار
   می‌کرد، برنامه‌اش دور انداخته می‌شد.

3. **باگ واقعی در تقویم شمسی:** الگوریتم تبدیل تاریخ در بعضی سال‌های کبیسه (نزدیک
   نوروز) یک سال اشتباه محاسبه می‌کرد — مثلاً ۲۰ اسفند... ۲۰ مارس ۲۰۲۴ را ۱ فروردین
   ۱۴۰۲ حساب می‌کرد در حالی که ۱ فروردین ۱۴۰۳ درسته. امروز (۳۱ ژوئیه ۲۰۲۶) تصادفاً
   درست محاسبه می‌شد، برای همین حس می‌کردی داره درست کار می‌کنه.

4. **باگ جاوااسکریپت که کل بنر نصب iOS رو خراب می‌کرد:** یک رشته‌ی متنی با کوتیشن تک
   نوشته شده بود ولی داخلش دوباره از کوتیشن تک استفاده شده بود، که باعث خطای سینتکس
   می‌شد و نصب روی آیفون عملاً کار نمی‌کرد.

## چی درست شد

- **الگوریتم تقویم شمسی** با پیاده‌سازی دقیق (بر پایه‌ی جدول ۳۳ ساله‌ی صحیح) جایگزین شد.
  الان روز/ماه/سال در همه‌ی سال‌ها درسته، نه فقط تصادفاً امروز.
- **باگ بنر نصب iOS** رفع شد.
- **دستیار حالا واقعاً از Claude با tool-calling استفاده می‌کنه:** هر پیام (نه فقط
  پیام‌های عمومی) مستقیم می‌ره به `/api/chat`، همراه با تاریخچه‌ی واقعی مکالمه (نه یک
  آرایه‌ی خالی). Claude اگه اطلاعات کافی نداشته باشه سوال می‌پرسه؛ اگه داشته باشه،
  یک "پیشنهاد" ساختاریافته (وظیفه/برنامه ورزشی/برنامه مطالعاتی/درس زبان) برمی‌گردونه.
- **جریان تایید واقعی اضافه شد:** هر پیشنهاد دستیار به شکل یک کارت با دو دکمه
  «✅ تایید و افزودن» و «❌ رد» نشون داده می‌شه. فقط با تایید کاربر، داده وارد
  IndexedDB می‌شه — دقیقاً همون چیزی که خواسته بودی (برنامه‌ریزی → تاییدیه → اضافه‌شدن
  به بخش مربوطه).

## به‌روزرسانی: تعیین سطح قبل از آموزش زبان

طبق درخواستت، دستیار الان قبل از شروع آموزش زبان، اول واقعاً سطح کاربر رو می‌سنجه:

- اگه بگی می‌خوای یه زبان یاد بگیری ولی سطحت رو نگفته باشی، دستیار (Claude) خودش
  ۶ تا ۸ سوال چهارگزینه‌ای با سختی افزایشی می‌سازه (ابزار جدید `start_placement_test`).
- این سوالات به‌صورت یک آزمون واقعی (رادیو-باتن + دکمه ثبت) توی چت نشون داده می‌شه.
- بعد از ثبت پاسخ‌ها، نتیجه (چند تا از چند تا درست بود) خودکار به دستیار فرستاده
  می‌شه؛ دستیار طبق درصد پاسخ درست سطح واقعی (A1 تا C1) رو تخمین می‌زنه و بلافاصله
  اولین درس رو دقیقاً متناسب با همون سطح می‌سازه — بدون این‌که دوباره سطح رو بپرسه.
- اگه از قبل بگی «سطحم B1 هست»، آزمون رد می‌شه و مستقیم می‌ره سراغ درس.

## چیزهایی که هنوز باقی مونده (نیاز به اولویت‌بندی داره)

- **آموزش زبان قوی‌تر از Duolingo**: الان زیرساخت AI برای ساخت درس پویا آماده شده،
  ولی محتوای واقعی (تلفظ صوتی، تمرین‌های تعاملی متنوع، spaced repetition واقعی برای
  لغات) کار جداگانه‌ایه — این خودش چند جلسه کار می‌بره.
- تست کامل روی سرور واقعی با کلید API (اینجا فقط کد رو خوندم، ویرایش کردم و از نظر
  سینتکس تست کردم؛ اجرای زنده نیاز به Docker/سرور خودت داره).
